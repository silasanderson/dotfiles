alias vimwiki="$EDITOR ~/vimwiki/index.wiki"

alias myip="curl ipinfo.io/ip"

alias mutt="pass mw-silas.anderson00@gmail.com && mw -Y && neomutt"

alias wall-black="xwallpaper --center download/black.png"

alias lf="lf"

alias lynx="lynx -vikeys"

alias vim="nvim"

alias P="doas pacman"

alias us="setxkbmap us"

alias colemakwide="setxkbmap colemak_wide_alt"

alias colemak="setxkbmap colemak_wide"

alias ek="nvim /usr/share/X11/xkb/symbols/colemak_wide"

alias ep="$EDITOR /etc/profile"

alias ex="$EDITOR ~/.xinitrc"

alias ez="$EDITOR ~/.zshrc"

alias eq="$EDITOR ~/.config/qutebrowser/config.py"

alias elf="$EDITOR ~/.config/lf/lfrc"

alias ev="$EDITOR ~/.config/nvim/init.vim"

alias ea="$EDITOR ~/.config/aliasrc"

alias es="$EDITOR ~/.config/sxhkd/sxhkdrc"

alias est="cd ~/repo/st/st-new-start/ && $EDITOR ~/repo/st/st-new-start/config.def.h"

alias edwm="cd ~/repo/dwm/dwm/ && $EDITOR ~/repo/dwm/dwm/config.def.h"

alias ec="edit-command-line"

alias psyu="doas pacman -Syu"

alias fet="fet.sh"

# switch between shells
alias tobash="sudo chsh $USER -s /bin/bash && echo 'Now log out.'"
alias tozsh="sudo chsh $USER -s /bin/zsh && echo 'Now log out.'"
alias tofish="sudo chsh $USER -s /bin/fish && echo 'Now log out.'"

# navigation
alias ..='cd ..' 
alias ...='cd ../..'


# broot
alias br='br -dhp'
alias bs='br --sizes'

# alias ls="ls --color=auto" */
alias lt="tree -CA"
alias tree="tree -CA"
alias grep="grep --color=auto"

# adding flags
alias cp="cp -i"                          # confirm before overwriting something
alias df='df -h'                          # human-readable sizes

# the terminal rickroll
alias rr='curl -s -L https://raw.githubusercontent.com/keroserene/rickrollrc/master/roll.sh | bash'

# termbin
alias tb="nc termbin.com 9999"

