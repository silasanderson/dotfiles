#!/bin/sh

if [[ $@ = *y* ]]; then curl 'https://corona-stats.online/states/us?minimal=true' -o ~/.cache/corona && clear ;fi

case $1 in
		 '')
				cat	~/.cache/corona
				;;
		*ma*)
				grep -i  ~/.cache/corona
				;;
esac

